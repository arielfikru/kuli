#!/usr/bin/env python3
"""KULI launcher for ask-deepseek-batch — thin entry into kuli.deepseek_batch:main."""
import os
import sys

_bin = os.path.dirname(os.path.realpath(__file__))
sys.path.insert(0, os.path.join(_bin, "..", "lib"))
from kuli.deepseek_batch import main

main()
